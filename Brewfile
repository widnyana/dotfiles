# macOS system packages — consumed by `brew bundle` from bin/install-dotfiles.sh.
#
# Rule: mise owns every CLI it can install (see config/mise/config.toml). This
# file is only for what mise should NOT own — GPG, casks, fonts, GUI apps.
# See packages/README.md.

brew "gnupg"
brew "pinentry-mac"

# casks / fonts / GUI — add as needed, e.g.:
# cask "sourcetree"
# cask "font-jetbrains-mono-nerd-font"
