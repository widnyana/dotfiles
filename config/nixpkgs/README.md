# nixpkgs

> **Non-functional.** `flake.lock` is missing and `flake.nix` may be corrupt, so
> this flake cannot be evaluated. Kept for reference only; nothing in the
> installer or shell chain invokes it. See the backlog in `FEATURE_FLAGS.md` —
> fix or remove in a dedicated change.


## Starterkit

This config is using https://github.com/dustinlyons/nixos-config; with home-manager disabled, and some configs were removed.