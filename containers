### Minikube
if [[ -x "$(command -v minikube)" ]]; then
    export MINIKUBE_HOME=/mnt/infra/.minikube
    export MINIKUBE_IN_STYLE=false
fi


export PATH=$PATH:${HOME}/.krew/bin

### Podman
#alias docker=podman
#alias docker-compose=podman-compose


. $(pack completion --shell zsh)

