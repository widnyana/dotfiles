## force consistent TERM outside tmux; tmux manages TERM itself via default-terminal
if [[ -z $TMUX ]]; then
  export TERM=xterm-256color
fi

## ohmyzsh - tmux plugin
export ZSH_TMUX_AUTOSTART_ONCE=false
export ZSH_TMUX_UNICODE=true

