# Repo-managed ~/.zshrc. bin/install-dotfiles.sh links this to ~/.zshrc.
#
# This file is oh-my-zsh setup ONLY. Everything portable — prompt, locale,
# editor, PATH, tool init — lives in the ${DOT_DIR} load chain, sourced from
# ~/.oh-my-zsh/custom/loader.zsh by `source $ZSH/oh-my-zsh.sh` below. Machine-
# specific lines go in ~/.zshrc.local (git-ignored), sourced at the end.

# Path to your oh-my-zsh installation.
export ZSH="${HOME}/.oh-my-zsh"

# mise env — MUST be set before `source $ZSH/oh-my-zsh.sh`: the oh-my-zsh `mise`
# plugin runs `mise activate` during that call, and the ${DOT_DIR} load chain
# (where the rest of the portable config lives) is sourced too late to affect it.
# Without MISE_DATA_DIR here, `mise activate` uses the default data dir and any
# `core` init gated on a mise-managed tool (starship prompt, fzf, atuin) silently
# no-ops on the first login shell. Keep MISE_DATA_DIR in sync with bin/install-dotfiles.sh.
export MISE_DATA_DIR="${HOME}/Development/sdks/mise"
export MISE_ALWAYS_KEEP_DOWNLOAD=0
export MISE_NOT_FOUND_AUTO_INSTALL=true
export MISE_NODE_COREPACK=1
export MISE_POETRY_AUTO_INSTALL=1
export MISE_NPM_PACKAGE_MANAGER=aube

# Theme is set by `omz`; the prompt is Starship (see ${DOT_DIR}/core).
ZSH_THEME=""

CASE_SENSITIVE="false"
DISABLE_AUTO_TITLE="true"

zstyle ':omz:update' mode reminder
zstyle ':omz:update' frequency 13
zstyle ':completion:*' menu select

plugins=(
    git
    fzf
    themes
    mise
    direnv
    docker
)

fpath+=${ZSH_CUSTOM:-${ZSH:-~/.oh-my-zsh}/custom}/plugins/zsh-completions/src

source $ZSH/oh-my-zsh.sh
## loader.zsh (the ${DOT_DIR} chain) is sourced from here, via
## $OMZ_PATH/custom/loader.zsh — managed by bin/install-dotfiles.sh

autoload -U +X bashcompinit && bashcompinit -i
autoload -U +X compinit && compinit -i

# Machine-local overrides (git-ignored; seeded from dot-zshrc.local.example)
[ -f ~/.zshrc.local ] && source ~/.zshrc.local
